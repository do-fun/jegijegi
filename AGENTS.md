# JEGI JEGI

OpenAI Game Builders Seoul 2026 출품을 위한 제기차기 기반 웹 아케이드 게임이다.
심사위원이 별도 설치나 승인 없이 URL에 접속해 바로 플레이할 수 있어야 한다.

## Product Goals

- 첫 입력 후 즉시 플레이를 시작할 수 있는 짧고 직관적인 아케이드 게임을 만든다.
- 핵심 재미는 좌우 이동으로 제기의 낙하 지점을 맞추고, 적절한 타이밍에 직접 차서 플레이를 이어가는 데 둔다.
- 한 판의 규칙과 조작은 별도 설명 없이 짧은 시간 안에 이해할 수 있어야 한다.
- 작동성, 독창성, Codex 협업 과정, 출시 가능성, 발표력을 주요 품질 기준으로 삼는다.
- 게임은 정적 웹 빌드로 배포 가능해야 하며 로그인이나 백엔드에 의존하지 않는다.

## Technology

- TypeScript
- Phaser
- Vite
- HTML/CSS
- Cloudflare static hosting
- `localStorage` for local settings and best scores when persistence is needed

React, 별도 백엔드, 데이터베이스, 추가 물리 엔진은 명확한 필요가 생기기 전에는 도입하지 않는다.
렌더링, 입력, 사운드, 애니메이션과 기본 물리는 우선 Phaser 기능으로 해결한다.

## Setup and Commands

프로젝트 스캐폴딩 이후 아래 명령을 표준으로 유지한다.

- Install: `npm install`
- Dev: `npm run dev`
- Type-check: `npm run typecheck`
- Test: `npm test`
- Build: `npm run build`
- Preview production build: `npm run preview`
- Deploy to Cloudflare: `npm run deploy` after the Cloudflare configuration is added

명령이 추가되거나 변경되면 `package.json`과 이 문서를 함께 갱신한다.

## Screen Flow

사용자에게 보이는 핵심 화면은 두 개만 둔다.

1. Title screen
   - 게임 로고 또는 게임명을 표시한다.
   - PC에서는 `아무 키나 누르세요`, 터치 기기에서는 `화면을 터치하세요`에 해당하는 안내를 표시한다.
   - 첫 키보드 입력이나 터치 입력으로 게임을 시작한다.
   - 첫 입력은 브라우저 오디오 재생 권한 활성화에도 사용한다.
2. Game screen
   - 실제 제기차기 플레이와 최소한의 HUD를 표시한다.
   - HUD에는 현재 점수, 콤보, 남은 생명, 현재 스테이지와 남은 시간을 표시한다.
   - 바람이 적용되면 현재 방향을 화살표로 표시한다.
   - 스테이지 클리어 안내는 별도 페이지가 아니라 게임 화면 위의 짧은 오버레이로 표시한다.
   - 게임 종료 결과는 별도 페이지 대신 게임 화면 위 오버레이로 표시한다.
   - 생명 소진 시 `GAME OVER`, 10 스테이지 완료 시 `ALL CLEAR`를 표시한다.
   - 결과에는 이번 점수, 최고 점수, 도달 스테이지와 이번 최대 콤보를 표시한다.
   - 결과 오버레이에서 키보드 입력이나 터치로 즉시 재시작할 수 있어야 한다.

에셋 로딩을 위한 Boot scene은 내부 구현으로 둘 수 있지만 사용자에게 별도 메뉴처럼 보이지 않아야 한다.
설정, 스테이지 선택, 계정, 상점 같은 화면은 현재 범위에 포함하지 않는다.

## Controls

PC 브라우저의 키보드 조작을 우선한다.

- Move left: `ArrowLeft`
- Move right: `ArrowRight`
- Kick: `Space`
- Toggle mute: `M`
- Start or restart: any keyboard key

모바일은 별도 게임 모드가 아니라 동일한 플레이를 위한 보조 입력을 제공한다.

- 모바일과 태블릿은 세로 모드만 지원한다.
- 모바일이나 태블릿이 가로 모드이면 게임 진행을 멈추고 기기를 세로로 돌리라는 안내를 표시한다.
- 화면 왼쪽 영역을 누르면 왼쪽으로 이동한다.
- 화면 오른쪽 영역을 누르면 오른쪽으로 이동한다.
- 캐릭터를 누르면 제기를 찬다.
- 캐릭터의 터치 영역은 좌우 이동 영역보다 우선하며, 차기와 이동이 동시에 입력되지 않도록 한다.
- HUD의 스피커 아이콘을 누르면 음소거를 전환한다.
- 좌우 이동 영역은 모바일에서만 반투명하게 표시할 수 있다.
- 키보드와 터치 입력은 게임 로직에서 직접 처리하지 않고 공통 action 상태로 변환한다.

차기 입력과 실패 판정은 다음 규칙을 따른다.

- 차기는 키 또는 캐릭터를 누른 순간 한 번만 발동하며, 입력을 유지해도 반복되지 않는다.
- 발은 캐릭터의 허리 높이까지 올라가며, 차기 모션의 초기 기준 시간은 250ms로 한다.
- 발끝 타점 원의 중심과 제기 중심 사이의 2차원 거리가 70px 이내일 때만 충돌로 판정한다.
- 판정 거리는 화면 표시 좌표가 아니라 고정 경기장의 논리적 좌표를 기준으로 계산한다.
- 유효 거리 안에서 발이 상승하는 동안 충돌하면 Good, 최고점 전후 25ms 동안 충돌하면 Perfect로 판정한다.
- 최고점 Perfect 구간이 끝난 뒤 발이 내려와 기본 자세로 돌아가는 동안에는 타격 판정을 적용하지 않는다.
- 차기 모션이 끝날 때까지 좌우로 이동하거나 다시 찰 수 없다.
- 차기 모션 중 제기와 충돌하지 않으면 헛차기로 처리하며 생명은 즉시 감소하지 않는다.
- 제기가 바닥에 떨어졌을 때만 차기 실패로 판정하고 생명 한 개를 감소시킨다.
- 70px 판정 반경은 초기값이며 플레이 테스트 후 조정한다.

입력 장치에 따라 핵심 규칙이나 점수 체계가 달라지지 않도록 한다.

벌레와 날씨의 무작위 방해는 다음 안전장치를 적용한다.

- 벌레 충돌은 상승·하강 시간, 높이와 진행 상태에 영향을 주지 않고 수평 궤도만 좌우 무작위로 15도 변경한다.
- 누적 궤도 각도는 수직선 기준 좌우 45도를 넘지 않도록 제한한다.
- 벌레 충돌 후 250ms 동안 다른 벌레와 충돌하지 않으며, 같은 벌레는 한 번의 비행에 한 번만 충돌한다.
- 한 번의 비행은 차기 성공 후부터 다음 차기 성공 또는 제기가 바닥에 떨어질 때까지로 정의한다.
- 벌레의 하단 등장 및 이동 경계는 `화면 높이 - (캐릭터 전체 높이 + 캐릭터 상반신 높이)`를 초기 기준으로 하며 플레이 테스트 후 조정한다.
- Perfect로 찬 제기는 상승하는 동안 벌레와 바람의 영향을 받지 않는다.
- 바람은 지속 가속하지 않고 상승 구간 시작과 하강 구간 시작에 각각 한 번씩 바람 방향으로 궤도를 변경한다.
- 바람의 궤도 변경은 회당 15도를 초기값으로 하고 누적 각도를 좌우 45도로 제한하며 플레이 테스트 후 조정한다.
- Perfect는 상승 구간의 바람 영향을 받지 않고 하강 구간에서만 한 번 영향을 받는다.
- 비는 상승 구간 시간과 최고 높이를 기본값의 2/3로 줄이고, 하강 속도를 기본값보다 1/3 높이는 것을 초기 기준으로 한다.
- 두 가지 날씨의 동시 적용은 후반 스테이지에서만 허용하며, 7 스테이지를 초기 기준으로 한다.

기본 제기 궤도는 다음 규칙을 따른다.

- Good은 매 차기마다 좌우 중 한 방향을 무작위로 선택해 수직선 기준 15도로 상승한다.
- Perfect는 수직으로 상승한다.
- Good과 Perfect는 동일한 상승 시간, 하강 시간과 최고 높이를 사용한다.
- 방해 요소가 없는 기본 체공 시간은 차기 성공 후 다음 차기 지점으로 돌아오기까지 1.2~1.5초를 초기 기준으로 한다.
- 상승 시간과 하강 시간은 각각 0.6~0.75초를 초기 기준으로 한다.
- 기본 최고점은 천장보다 낮게 설정하고 현재 방해 요소는 최고 높이를 높이지 않으므로 정상 플레이에서는 천장 충돌이 발생하지 않게 한다.
- 천장 충돌 규칙은 물리 수치 조정과 차후 방해 효과에 대비한 안전 경계로 유지한다.
- 실제 중력을 그대로 시뮬레이션하지 않고 상승, 최고점과 하강 구간별 곡선으로 궤도를 계산한다.
- 상승은 점점 느려지고 하강은 점점 빨라지도록 표현하며, 구간 시간, 최고 높이와 수평 이동량은 플레이 테스트 후 조정한다.

경기장 경계 충돌은 다음 규칙을 따른다.

- 벽 충돌 시 구간 시간과 진행 상태 및 궤도 각도를 유지하고 수평 방향만 반전한다.
- 천장 충돌 시 좌우 방향과 궤도 각도를 유지하고 상승 구간을 종료한 뒤 하강 구간으로 전환한다.
- 벽과 천장 충돌은 점수와 생명에 영향을 주지 않는다.
- 바닥에서는 제기를 반사하지 않고 차기 실패로 판정해 생명 한 개를 감소시킨다.
- Perfect 상승 중에도 벽과 천장에는 정상적으로 충돌하며, 천장 충돌 후 하강이 시작되면 벌레와 바람의 영향을 받는다.

점수와 콤보는 다음 규칙을 따른다.

- Good은 50점, Perfect는 100점이다.
- 연속 Perfect는 두 번째부터 고정 100점 보너스를 더해 회당 200점을 지급한다.
- Good 또는 Perfect 성공 시 콤보가 증가하고, Good은 연속 Perfect 횟수만 초기화한다.
- 헛차기는 점수, 콤보와 연속 Perfect 횟수에 영향을 주지 않는다.
- 제기가 바닥에 떨어지면 콤보와 연속 Perfect 횟수를 초기화한다.
- 스테이지 전환만으로 콤보와 연속 Perfect 횟수를 초기화하지 않는다.
- 한 비행에서 충돌한 서로 다른 벌레 한 마리당 50점을 다음 성공 차기에 지급하며, 최대 세 마리 150점으로 제한한다.
- 다음 차기 전에 제기가 바닥에 떨어지면 지급되지 않은 벌레 보너스는 소멸한다.
- 스테이지 클리어와 남은 생명에는 별도 점수 보너스를 지급하지 않는다.

플레이 안내와 피드백은 다음 원칙을 따른다.

- 별도 튜토리얼 화면 없이 첫 차기 성공 또는 게임 시작 약 3초까지 이동, 차기와 음소거 조작 안내를 표시한다.
- PC 안내에는 `M` 키 음소거를 포함한다.
- 모바일·태블릿에서는 이동과 차기 영역을 명확하게 안내하고, HUD의 스피커 아이콘은 설명 문구 없이 한 번만 가볍게 강조한다.
- Good과 Perfect 판정 및 획득 점수를 제기를 찬 위치 근처에 잠시 표시한다.
- Perfect에는 30~50ms의 짧은 정지 효과를 초기값으로 적용하고 플레이 테스트 후 조정한다.
- 남은 시간이 5초 이하이면 타이머를 강조한다.
- 3, 6, 9 스테이지의 생명 회복과 새로운 방해 요소 등 중요한 상태 변화를 짧은 오버레이로 알린다.
- 스테이지 전환 오버레이가 표시되는 동안에는 타이머와 게임 물리를 정지한다.

사운드는 다음 원칙을 따른다.

- 첫 키보드 또는 터치 입력으로 브라우저 오디오 재생 권한을 활성화한다.
- 배경 음악 한 곡과 제한된 반복 주변음을 사용하고, 판정·충돌·실패·스테이지 결과 효과음을 우선한다.
- Good과 Perfect는 화면을 보지 않아도 구분되는 타격음을 사용하며, 연속 Perfect 강조음은 연속 Perfect 횟수 초기화 시 원래 음높이로 돌아간다.
- 좌우 이동 반복음과 파리별 반복음은 사용하지 않는다.
- 파리 주변음은 하나만 재생하고, 비와 바람이 동시에 적용되면 각 주변음의 음량을 낮춘다.
- 남은 시간 5초 경고음은 한 번만 재생한다.
- PC의 `M` 키와 모바일·태블릿 HUD의 스피커 아이콘으로 음소거를 전환하고 상태를 `localStorage`에 저장한다.
- 브라우저가 포커스를 잃으면 배경 음악과 반복 주변음을 일시 정지한다.
- 햅틱과 진동 피드백은 첫 버전에 적용하지 않는다.

## Project Structure

구현을 시작할 때 다음 구조를 기본으로 사용하되 실제 필요에 따라 단순화할 수 있다.

```text
src/
  main.ts
  game/
    config.ts
    scenes/
      BootScene.ts
      TitleScene.ts
      GameScene.ts
    objects/
      Player.ts
      Jegi.ts
    systems/
      InputManager.ts
      ScoreManager.ts
      AudioManager.ts
  styles/
    main.css
public/
  assets/
```

- Scene은 화면 흐름과 수명 주기를 조정한다.
- 게임 규칙과 계산을 Phaser 표시 객체 안에 과도하게 섞지 않는다.
- 키보드와 터치의 차이는 `InputManager` 경계 안에서 처리한다.
- 재사용되지 않는 작은 로직을 성급하게 별도 시스템이나 클래스로 추출하지 않는다.

## Hosting and Deployment

- 배포 플랫폼은 Cloudflare를 사용한다.
- 게임은 Vite의 `dist/` 정적 빌드 결과물만으로 실행 가능해야 한다.
- Cloudflare 설정은 저장소에서 재현 가능하도록 Wrangler 설정과 `package.json` 스크립트로 관리한다.
- 기존에 운영 중인 agentinit 사이트와 같은 Cloudflare 계정 및 운영 방식을 우선 참고하되, 프로젝트 이름과 배포 대상은 분리한다.
- 로컬 개발 환경이나 비공개 파일에만 존재하는 설정에 의존하지 않는다.
- 해시가 포함된 정적 에셋은 장기 캐시할 수 있지만 `index.html`은 새 배포가 즉시 반영되도록 과도하게 캐시하지 않는다.
- 배포 URL에서 루트 접속, 새로고침, 에셋 로딩, 키보드 입력과 터치 입력을 다시 검증한다.

## Code Style and Architecture

- TypeScript strict mode를 유지하고 불가피한 경우가 아니면 `any`를 사용하지 않는다.
- 기존 파일의 패턴을 우선하며 추상화는 실제 중복이나 교체 필요가 있을 때 추가한다.
- 게임 플레이에 영향을 주는 숫자는 의미 있는 이름의 상수나 설정으로 관리한다.
- Scene 간에 암묵적인 전역 상태를 공유하지 않는다.
- 게임 상태는 재시작할 때 완전히 초기화되어야 한다.
- 데스크톱과 모바일 레이아웃에서 플레이 영역이 잘리거나 입력 영역이 어긋나지 않게 한다.
- 화면 크기와 비율에 관계없이 실제 경기장의 논리적 너비와 높이를 고정한다.
- 데스크톱의 넓은 화면에서 남는 공간은 배경, 장식 또는 HUD에만 사용하며 이동 범위와 게임 물리를 확장하지 않는다.
- 모바일과 태블릿의 가로 모드에서는 게임 화면 대신 세로 모드 전환 안내가 표시되어야 한다.
- 브라우저 크기 변경과 고해상도 디스플레이를 고려하되, 시각적 완성도보다 플레이 안정성을 우선한다.
- 사용자에게 표시되는 문구는 한 언어 안에서 일관성을 유지한다.

## Testing and Validation

모든 변경은 최소한 다음을 확인한다.

- TypeScript type-check가 통과한다.
- 프로덕션 빌드가 성공한다.
- 타이틀 화면에서 키보드와 터치로 게임을 시작할 수 있다.
- 좌우 동시 입력, 입력 해제, 브라우저 포커스 이탈 후에도 이동 상태가 고정되지 않는다.
- 키보드와 터치에서 차기 입력이 한 번의 동작으로 한 번만 처리되며, 키를 누르고 있는 동안 반복되지 않는다.
- 차기 모션 중에는 좌우 이동 입력이 캐릭터를 움직이지 않는다.
- 발의 상승, 최고점, 하강 구간에서 각각 Good, Perfect, 판정 없음이 올바르게 적용된다.
- 고정 경기장의 논리적 좌표에서 중심 거리가 70px 이하일 때만 차기 충돌이 성립한다.
- 헛차기만으로 생명이 감소하지 않으며, 제기가 바닥에 떨어질 때만 생명이 감소한다.
- 벌레 충돌 전후 상승·하강 시간, 높이와 진행 상태가 유지되고 수평 궤도 각도 제한이 적용된다.
- 벌레 충돌 재사용 및 250ms 연속 충돌 제한이 올바르게 초기화되고 적용된다.
- 벌레가 캐릭터 바로 위의 보호 영역에 등장하거나 진입하지 않는다.
- Perfect 상승 중에는 벌레와 바람의 영향을 받지 않는다.
- 바람은 상승·하강 구간마다 최대 한 번씩 적용되고 비는 상승 시간, 최고 높이와 하강 속도에 정해진 비율로 적용된다.
- 벽 충돌은 수평 방향만 반전하고 천장 충돌은 상승 구간을 종료해 하강 구간으로 전환한다.
- Perfect 제기가 천장에 충돌하면 하강 상태로 전환되고 벌레와 바람의 영향을 받는다.
- Good, Perfect, 연속 Perfect와 벌레 보너스 점수가 정의된 계산식과 일치한다.
- Good, 헛차기, 바닥 실패와 스테이지 전환에서 콤보 및 연속 Perfect 횟수가 정의된 규칙대로 유지되거나 초기화된다.
- 벌레 보너스는 서로 다른 벌레를 최대 세 마리까지 계산하고 다음 성공 또는 바닥 실패 후 올바르게 초기화된다.
- HUD에서 점수, 콤보, 생명, 스테이지와 남은 시간이 현재 게임 상태와 일치한다.
- 첫 플레이 조작 안내에 PC와 터치 기기의 음소거 방법이 표시된다.
- 바람 방향, 판정 결과, 생명 변화와 스테이지 전환 피드백이 올바르게 표시된다.
- 스테이지 전환 오버레이 중에는 타이머와 게임 물리가 진행되지 않는다.
- 생명 소진과 10 스테이지 완료 시 각각 `GAME OVER`와 `ALL CLEAR` 결과가 표시되고 입력으로 재시작할 수 있다.
- Good, Perfect, 경계·벌레 충돌, 실패와 스테이지 결과 효과음이 해당 상태와 일치한다.
- 반복 주변음이 중복 생성되지 않고 중요 판정음을 가리지 않는다.
- 음소거 상태가 새로고침 후에도 유지되며 브라우저 포커스 이탈 시 반복 사운드가 정지한다.
- 시작 또는 재시작에 사용한 입력이 게임 시작 직후의 차기 입력으로 이어지지 않는다.
- 게임 종료 후 재시작하면 점수, 콤보, 제기와 플레이어 상태가 초기화된다.
- 새로고침 후에도 별도 로그인 없이 플레이할 수 있다.
- 일반적인 데스크톱 화면과 모바일·태블릿 세로 화면에서 게임 영역을 확인한다.
- 모바일·태블릿 가로 화면에서는 세로 모드 전환 안내가 표시되고, 방향 전환 전의 이동 및 차기 입력 상태가 남지 않는다.
- 브라우저 콘솔에 처리되지 않은 오류가 없어야 한다.

게임 물리나 점수 계산처럼 Phaser 렌더링 없이 검증 가능한 규칙은 순수 함수로 분리하고 단위 테스트를 추가한다.
시각적 또는 입력 변경은 자동 테스트만으로 완료하지 말고 실제 브라우저 플레이로 확인한다.

## Workflow

- 먼저 가장 작은 플레이 가능한 루프를 완성한 뒤 연출, 사운드, 추가 규칙을 더한다.
- 게임의 핵심 조작이나 화면 흐름을 바꿀 때는 구현 전에 이 문서와 일치하는지 확인한다.
- Codex가 구현한 기능, 해결한 문제, 사용자가 직접 결정한 부분을 제출 자료로 정리할 수 있게 개발 기록을 남긴다.
- 기존 프로젝트를 활용하게 되는 경우 대회 기간에 새로 개발한 범위를 명확히 기록한다.
- Cloudflare 배포는 프로덕션 빌드를 로컬에서 검증한 뒤 진행한다.

## Security and Boundaries

- 비밀 값, 인증 정보, 개인 정보와 `.env` 파일을 커밋하지 않는다.
- 게임 실행을 위해 외부 계정, 로그인, 추적 스크립트를 요구하지 않는다.
- 외부 에셋과 폰트는 사용 권리와 라이선스를 확인하고 출처가 필요한 경우 기록한다.
- Cloudflare 외의 배포 대상과 새로운 외부 서비스, 유료 API, 분석 도구 또는 백엔드 추가는 사용자와 먼저 합의한다.
- 실제 배포와 외부 서비스 변경은 사용자가 명시적으로 요청한 경우에만 수행한다.
